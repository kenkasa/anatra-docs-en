## Restricted Radial Potential of Mean Force (``restricted_radial``, ``rrpmf``)

This analysis mode calculates the free energy profile along the radial direction under a condition defined by one or more reaction coordinates (collective variables).

Using this profile, the free energy change associated with the binding of a host molecule and a guest molecule (binding free energy) can also be evaluated.

### Theoretical Background

Let ${\bf X}$ denote the configuration of the system.
The intermolecular distance between the host and guest molecules and the reaction coordinates defining the binding state are denoted by $d\left({\bf X}\right)$ and $\bm{\xi}\left({\bf X}\right)$, respectively.

The potential of mean force along the intermolecular distance is defined as

\begin{align}
w\left(r\right) =-\dfrac{1}{\beta}\log\left[\int d{\bf X}\,\dfrac{\delta\left(r-d\left({\bf X}\right)\right)}{4\pi r^{2}}\exp\left(-\beta U\left({\bf X}\right)\right)\right]+C
\end{align}

Furthermore, let $\bm{\Upsilon}$ be the region in the reaction coordinate space corresponding to the bound state, and introduce the characteristic function $\Theta\left(\bm{\xi}\right)$ defined as

\begin{align}
\Theta_{\mathrm{B}}\left(\bm{\xi}\right) =\begin{cases}
1 & \bm{\xi}\in\bm{\Upsilon}\\
0 & \bm{\xi}\notin\bm{\Upsilon}
\end{cases}
\end{align}

Using this function, the PMF for configurations belonging to the bound state is expressed as

\begin{align}
w_{\mathrm{B}}\left(r\right) =-\dfrac{1}{\beta}\log\left[\int d{\bf X}\,\dfrac{\delta\left(r-d\left({\bf X}\right)\right)}{4\pi r^{2}}\Theta_{\mathrm{B}}\left(\bm{\xi}\left({\bf X}\right)\right)\exp\left(-\beta U\left({\bf X}\right)\right)\right]+C
\end{align}

The constant $C$ appearing in both $w\left(r\right)$ and $w_{\mathrm{B}}\left(r\right)$ is taken to be the same value, and is chosen such that the minimum value of $w_{\mathrm{B}}\left(r\right)$ becomes zero.

Since $w\left(r\right)$ becomes constant when $r$ is sufficiently large, let this constant be $\Delta w$, and define the region satisfying $w\left(r\right)=\Delta w$ as $r_{0}\leq r\leq r_{1}$.
The choice of $r_{0}$ and $r_{1}$ is arbitrary as long as this condition is satisfied.
This point will be discussed later.

The probability ratio between the unbound state U, where the two molecules are separated by $r_{0}\leq r \leq r_{1}$, and the bound state B is

\begin{align}
\dfrac{{\displaystyle \int_{\bm{\Upsilon}}dr\,\exp\left(-\beta w_{\mathrm{B}}\left(r\right)\right)}}{{\displaystyle \int_{r_{0}}^{r_{1}}dr\,\exp\left(-\beta w\left(r\right)\right)}}
\end{align}

Therefore, the free energy change $\Delta G_{\mathrm{PMF}}$ associated with the transition from U to B is

\begin{align}
\Delta G_{\mathrm{PMF}} =-\dfrac{1}{\beta}\log\dfrac{{\displaystyle \int_{\bm{\Upsilon}}dr\,4\pi r^{2}\exp\left(-\beta w_{\mathrm{B}}\left(r\right)\right)}}{{\displaystyle \int_{r_{0}}^{r_{1}}dr\,4\pi r^{2}\exp\left(-\beta w\left(r\right)\right)}}
\end{align}

Since $w\left(r\right)=\Delta w$ within the range $r_{0}\leq r\leq r_{1}$, this equation can be rewritten as

\begin{align}
\Delta G_{\mathrm{PMF}} =-\Delta w-\dfrac{1}{\beta}\log\dfrac{V_{\mathrm{B}}}{V_{\mathrm{U}}}
\label{pmf:dGpmf}
\end{align}

where

\begin{align}
V_{\mathrm{B}} =\int_{\bm{\Upsilon}}dr\,4\pi r^{2}\exp\left(-\beta w_{\mathrm{B}}\left(r\right)\right)\\
V_{\mathrm{U}} =\int_{r_{0}}^{r_{1}}dr\,4\pi r^{2}
\end{align}

The second term in Eq. EQREF(pmf:dGpmfEQREF) represents the free energy change associated with the transition of the guest molecule from a concentration of $1/V_{\mathrm{U}}$ to a concentration of $1/V_{\mathrm{B}}$.

The experimentally measured binding free energy is the free energy change associated with binding a guest molecule at the standard-state concentration (typically $c^{\circ} = 1~\mathrm{mol~L^{-1}}$), and is therefore different from $\Delta G_{\mathrm{PMF}}$.

Let $V^{\circ}$ be the volume corresponding to a concentration of $c^{\circ}$ for a single guest molecule.
Then $V^{\circ} = 1661$ ANGSTROM$^{3}$.

The free energy change $\Delta G^{\circ}_{\mathrm{corr}}$ associated with the transition from concentration $c^{\circ}=1/V^{\circ}$ to $1/V_{\mathrm{U}}$ is

\begin{align}
\Delta G^{\circ}_{\mathrm{corr}} = - \dfrac{1}{\beta} \log \dfrac{V_{\mathrm{U}}}{V^{\circ}}
\label{pmf:dGcorr}
\end{align}

Thus, by decomposing the transition from $1/V^{\circ}$ to $1/V_{\mathrm{B}}$ into the two steps,

\begin{align}
1/V^{\circ} \to 1/V_{\mathrm{U}}
\end{align}

and

\begin{align}
1/V_{\mathrm{U}} \to 1/V_{\mathrm{B}}
\end{align}

we obtain

\begin{align}
\Delta G^{\circ} = \Delta G_{\mathrm{PMF}} + \Delta G_{\mathrm{corr}}^{\circ}
\end{align}

Substituting Eqs. EQREF(pmf:dGpmfEQREF) and EQREF(pmf:dGcorrEQREF) into the above equation yields

\begin{align}
\Delta G^{\circ} = - \Delta w - \dfrac{1}{\beta} \log \dfrac{V_{\mathrm{B}}}{V^{\circ}}
\end{align}

Since this expression does not contain $V_{\mathrm{U}}$, the value of $\Delta G^{\circ}$ is independent of the choice of $r_{0}$ and $r_{1}$.

### Parameter List

**Namelist**      | **Parameter**      | **Description**
:-----------------|:-------------------|:----------------------------------------------------------------
`&input_param`    | `flist_cv`         | File containing the list of CV files
`&input_param`    | `flist_weight`     | File containing the list of weight files (optional)
`&input_param`    | `fcv`              | CV filename
`&input_param`    | `fweight`          | Weight filename
`&output_param`   | `fhead`            | Header of the output filename
`&option_param`   | `calcfe`           | Whether to calculate the standard free energy
`&option_param`   | `ndim`             | Number of CV dimensions
`&option_param`   | `only_r`           | Whether the intermolecular distance is the only CV
`&option_param`   | `temperature`      | Temperature [K]
`&option_param`   | `dr`               | Distance grid spacing
`&option_param`   | `ngrid`            | Number of distance grids
`&option_param`   | `nsta`             | Starting frame number for the analysis
`&option_param`   | `use_bootstrap`    | Whether to perform bootstrap analysis
`&option_param`   | `vol0`             | Standard-state volume
`&option_param`   | `urange`           | Distance range of the unbound state
`&option_param`   | `bound_range`      | Distance range of the bound state
`&bootopt_param`  | `iseed`            | Random number seed
`&bootopt_param`  | `nsample`          | Number of samples drawn in each trial
`&bootopt_param`  | `duplicate`        | Whether duplicate sampling is allowed
`&bootopt_param`  | `ntrial`           | Number of bootstrap trials

<!---
```{raw} latex
\renewcommand{\arraystretch}{1.2}
{\arrayrulewidth=0.5pt
\begin{tabular}{p{3cm}p{5cm}p{7cm}}
\arrayrulecolor{gray}
\rowcolor{headerColor}
\hline
\textbf{Namelist} & \textbf{Parameter} & \textbf{Description}\\
\hline
\verb|&input_param|  & \verb|flist_cv|      & File containing the list of CV files \\
                     & \verb|flist_weight|  & File containing the list of weight files (optional) \\
                     & \verb|fcv|           & CV filename \\
                     & \verb|fweight|       & Weight filename \\
\hline
\verb|&output_param| & \verb|fhead|         & Header of the output filename \\
\hline
\verb|&option_param| & \verb|calcfe|        & Whether to calculate the standard free energy \\
                     & \verb|ndim|          & Number of CV dimensions \\
                     & \verb|only_r|        & Whether the intermolecular distance is the only CV \\
                     & \verb|temperature|   & Temperature [K] \\
                     & \verb|dr|            & Distance grid spacing \\
                     & \verb|ngrid|         & Number of distance grids \\
                     & \verb|nsta|          & Starting frame number for the analysis \\
                     & \verb|use_bootstrap| & Whether to perform bootstrap analysis \\
                     & \verb|vol0|          & Standard-state volume \\
                     & \verb|urange|        & Distance range of the unbound state \\
                     & \verb|bound_range|   & Distance range of the bound state \\
\hline
\verb|&bootopt_param| & \verb|iseed|        & Random number seed \\
                      & \verb|nsample|      & Number of samples drawn in each trial \\
                      & \verb|duplicate|    & Whether duplicate sampling is allowed \\
                      & \verb|ntrial|       & Number of bootstrap trials \\
\hline
\end{tabular}
}
```
--->

### Format of the Input File (Time-Series Data)

* 1st column: Time
* 2nd to $(N+1)$-th columns: CV values
* Last column: Intermolecular distance

The first column is ignored, and the second column is treated as the first data column.

If the intermolecular distance is the only CV, i.e.,

* 1st column: Time
* 2nd column: Intermolecular distance

then simply specify

```
only_r = .true.
```

in ``&option_param``.

### ``&option_param``

* **``calcfe``**
    * Default: ``.true.``
    * Example: ``calcfe = .true.``

    Specifies whether the standard binding free energy should be calculated.

    If ``.true.``, the standard binding free energy is calculated.
    If ``.false.``, only the PMF profile is generated.

----

* **``ndim``**
    * Default: None
    * Example: ``ndim = 1``

    Specifies the number of CV dimensions.

    Currently, both one-dimensional and multidimensional PMF calculations are supported.

----

* **``only_r``**
    * Default: ``.false.``
    * Example: ``only_r = .false.``

    Set this option to ``.true.`` when the intermolecular distance is the only CV and no additional CVs are present.

----

* **``temperature``**
    * Default: ``298.0``
    * Example: ``temperature = 300.0``

    Specifies the temperature $T$ [K] used for the free energy calculation.

----

* **``dr``**
    * Default: None
    * Example: ``dr = 0.1d0``

    Specifies the grid spacing along the distance coordinate.

    This value is used as the grid interval $\Delta r$.

----

* **``ngrid``**
    * Default: None
    * Example: ``ngrid = 1000``

    Specifies the number of grid points along the distance coordinate.

    The PMF is evaluated on ``ngrid`` grid points.

----

* **``nsta``**
    * Default: ``1``
    * Example: ``nsta = 100``

    Specifies the frame number from which the analysis starts.

    Frames before ``nsta`` are excluded from the analysis.

----

* **``use_bootstrap``**
    * Default: ``.false.``
    * Example: ``use_bootstrap = .true.``

    Specifies whether bootstrap error analysis is performed.

    If ``.true.``, bootstrap analysis is carried out according to the settings in ``&bootopt_param``.

----

* **``vol0``**
    * Default: ``1661.0d0``
    * Example: ``vol0 = 1661.0d0``

    Specifies the standard-state volume $V^\circ$.

    The default value corresponds to the volume for a concentration of 1 mol/L in ANGSTROM units (1661 ANGSTROM$^3$).

----

* **``urange``**
    * Default: None
    * Example: ``urange = 25.0 30.0``

    Specifies the distance range regarded as the unbound state.

    The first value represents the lower bound, and the second value represents the upper bound.

----

* **``bound_range``**
    * Default: None
    * Example:
      ```
      react_range = 0.0 10.0
                    5.0 20.0
      ```

    Specifies the CV range corresponding to the bound state.

    For each line, the first value represents the lower bound and the second value represents the upper bound.

    By specifying ``ndim`` ranges, a multidimensional bound region can be defined.

### ``&bootopt_param``

This namelist controls the settings for bootstrap error analysis.

It is enabled only when ``use_bootstrap = .true.``.

* **``iseed``**
    * Default: Automatically generated
    * Example: ``iseed = 3141592``

    Specifies the random seed used for bootstrap analysis.

    If ``iseed <= 0``, the program automatically generates a seed.

----

* **``nsample``**
    * Default: None
    * Example: ``nsample = 1000``

    Specifies the number of samples drawn in each bootstrap trial.

----

* **``duplicate``**
    * Default: ``.true.``
    * Example: ``duplicate = .false.``

    Specifies whether duplicate sampling is allowed during bootstrap sampling.

----

* **``ntrial``**
    * Default: None
    * Example: ``ntrial = 100``

    Specifies the number of bootstrap trials.

    Increasing the number of trials improves the accuracy of the statistical error estimate, but also increases the computational cost.

### Output File Format

* ``*.rd``
    * 1st column: Intermolecular distance $r$
    * 2nd column: Conventional PMF along $r$
    * 3rd column: PMF corresponding to the bound state

### Example

The following example calculates the binding free energy from a time-series data file ``inp.ts`` that contains one CV and the intermolecular distance in the last column.

The range $18 \leq r/$ANGSTROM$\leq 20$ is treated as the unbound state, and the bound state is defined by the CV range $5.0 \leq \zeta \leq 10.0$.

```bash
#!/usr/bin/env bash

cat << EOF > run.inp
&input_param
 fcv      = "inp.ts"
/
&output_param
 fhead         = "out"
/
&option_param
 calcfe        = .true.
 only_r        = .false.
 ndim          =  1
 temperature   =  300.0
 dr            =  0.1d0
 ngrid         =  1000
 nsta          =  1
 use_bootstrap = .false.
 vol0          = 1661.0d0
 urange        =  18.0  20.0
 bound_range   =  5.0  10.0
/
EOF

fanatra restricted_radial run.inp
```

After execution, the results are printed near the end of the log file.

```text
... Thermodynamic analysis ...
vb      (A^3)      =      21.5444908
wu      (kcal/mol) =      -1.3486059
wb      (kcal/mol) =       0.0000000
dw      (kcal/mol) =      -1.3486059
dGv     (kcal/mol) =       2.5903536
dGzero  (kcal/mol) =       3.9389595
Keq     (M^-1)     =   1.3506025E-03
```
